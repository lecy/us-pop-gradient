# us-pop-gradient shiny app

An app to demonstrate how much of the US population can be reached from a given country within a given distance.


```r

library(shiny)

runGitHub("us-pop-gradient", "lecy")


```
